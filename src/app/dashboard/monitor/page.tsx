import { ScraperMonitor } from "@/components/dashboard/ScraperMonitor";

export const metadata = {
  title: "Dashboard Monitor - Scraper",
};

export default function MonitorPage() {
  return <ScraperMonitor />;
}
